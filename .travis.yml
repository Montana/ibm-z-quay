language: generic

arch: ppc64le

services:
  - docker

script:
  - sudo systemctl start docker
  - docker pull quay.io/montana/ppc64le

deploy:
  provider: script
  script: 
  - bash build.sh
  
  on:
    branch: master
